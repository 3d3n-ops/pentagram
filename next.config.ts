import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["https://xuot59okd8gfywot.public.blob.vercel-storage.com"],
  },
};

export default nextConfig;
